@echo off

:: Navigate to the server directory and start the server
:: start cmd /k "cd /d server && npm start"

:: Navigate to the web-client directory and start the client
start cmd /k "cd /d web-client && npm run dev"